//WITH LINKED FIRMWARE IT WORKS WITH THE DEFAULT DRAW_OPT_ TOO!!!!

task main() {
  /*for(int i = 0; i <= INT_MAX; i++) {
   NumOut(0, LCD_LINE8, i);
   RectOut(DISPLAY_WIDTH/3, DISPLAY_HEIGHT/3, (DISPLAY_WIDTH/3)*2, (DISPLAY_HEIGHT/3)*2, i);
   while(ButtonPressed(BTNCENTER, false) == 0) { Wait(10); };
   while(ButtonPressed(BTNCENTER, false) == 1) { Wait(10); };

   Empty:
   5, 6, 7, 9, 10, 11, 13, 14, 15, 21, 22, 23, 29, 30, 31, 37, 38, 39, 40, 41, 42, 43, 45, 46, 47, 53, 54, 55, 61, 62, 63, 69, 70, 71, 73
   
   Just Number:
   4, 8, 12, 36, 44, 68, 72,
   
   Just Rect:
   1, 3, 17, 18, 19, 25, 26, 27, 33*, 34*, 35*, 49*, 50*, 51*, 57*, 58*, 59*, 65, 66, 67,
   
   Both:
   0, 16, 20, 24, 28, 32*, 48*, 52*, 56*, 60*, 64,
   
   *Filled
  };*/

 /*for(int i = 0; i <= INT_MAX; i++) {
  NumOut(0, LCD_LINE8, i);
  RectOut(DISPLAY_WIDTH/3, DISPLAY_HEIGHT/3, (DISPLAY_WIDTH/3)*2, (DISPLAY_HEIGHT/3)*2, 1<<i);
  while(ButtonPressed(BTNCENTER, false) == 0) { Wait(10); };
  while(ButtonPressed(BTNCENTER, false) == 1) { Wait(10); };
 };
 
  Empty:
  
  Just Number:
  2,3

  Just Rect:
  0,1

  Both:
  4,5*,6*,7*,8*,9*,...

   *Filled
 */
 
  /*for(int i = 0; i <= INT_MAX; i++) {
  NumOut(0, LCD_LINE8, i);
  RectOut(DISPLAY_WIDTH/3, DISPLAY_HEIGHT/3, (DISPLAY_WIDTH/3)*2, (DISPLAY_HEIGHT/3)*2, 1>>i);
  while(ButtonPressed(BTNCENTER, false) == 0) { Wait(10); };
  while(ButtonPressed(BTNCENTER, false) == 1) { Wait(10); };
  };
  Empty:

  Just Number:

  Just Rect:
  0
  Both:
  1,2,3,...

   *Filled
 */
}


